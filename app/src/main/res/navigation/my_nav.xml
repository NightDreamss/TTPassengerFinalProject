<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_nav"
    app:startDestination="@id/requestRide">
    <fragment
        android:id="@+id/requestRide"
        android:name="com.nightdream.ttpassenger.RequestRide"
        android:label="fragment_request_ride"
        tools:layout="@layout/fragment_request_ride" />
    <fragment
        android:id="@+id/rideRequests"
        android:name="com.nightdream.ttpassenger.RideRequests"
        android:label="fragment_ride_requests"
        tools:layout="@layout/fragment_ride_requests" />
</navigation>